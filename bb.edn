{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([riscv_isa.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'riscv_isa.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
